# ML model files

Place your trained artifacts here:

- `insurance_model.pkl`
- `column_transformer.pkl`

These must match the scikit-learn version in `requirements.txt` (used when the models were saved).

Do not commit `.env` or virtualenv folders. The `.pkl` files may be committed if they are small enough for GitHub (< ~50 MB each).
